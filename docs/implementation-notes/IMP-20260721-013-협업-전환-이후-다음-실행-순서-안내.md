# IMP-20260721-013 — 협업 전환 이후 다음 실행 순서 안내

- Date/Time (KST): 2026-07-21T16:28:46+09:00
- Task ID: STATUS-NEXT-001
- Type: documentation-handoff
- Status: Done — next sequence established; human approval/runtime evidence pending
- Author/Agent: Codex primary agent
- Branch: `codex/COLLAB-001-pr2-merge-evidence`
- Base commit: `df1eb7c887046ee0c13b71f4d38bfcb33efd81be`
- Related plan/ADR/RFP: COLLAB-001 approved plan, DATA-SEED-002 plan, ADR-0017/0019, D-044/D-047~D-057, `TASKS.md`

## 1. 사용자 요청과 완료 기준

### 요청

PR #3 병합·사후 검증 뒤 사용자가 이제 무엇을 해야 하는지 물었다.

### Acceptance Criteria

- 사용자·frontend 팀원·Codex의 다음 작업을 분리한다.
- 인간-only 항목은 제품 개발을 불필요하게 막지 않도록 병렬화한다.
- 현재 remote branch/PR 상태를 실제 GitHub 근거로 확인한다.
- note ID 충돌이나 오래된 base에서 PR을 만드는 위험을 먼저 차단한다.
- 다음 제품 수직 흐름과 필요한 승인 문구를 명확히 안내한다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자/저장소 owner, frontend collaborator `koregy`, primary Codex |
| When — 언제 | PR #3 post-merge PASS 뒤 2026-07-21 KST |
| Where — 어디서 | private GitHub `tskwak111/Sejong_AI`, Codex Cloud, local evidence branch |
| What — 무엇을 | 협업 잔여 gate와 다음 P0 제품 흐름의 안전한 실행 순서 확정 |
| Why — 왜 | 팀원 branch의 note-ID 충돌을 방지하면서 backend 제품 개발을 병렬로 재개하기 위해 |
| How — 어떻게 | `TASKS.md`/approved plans와 실제 remote PR/branch/commit diff를 대조 |
| How much — 어느 정도 | remote PR 3건, pending teammate branch 1개, 충돌 note ID 1개, 다음 제품 slice 1개 |

## 3. 시작 전 상태

- 관련 파일: `TASKS.md`, COLLAB-001 plan/handoff, DATA-SEED-002 spec/plan, implementation-note INDEX.
- 기존 동작: PR #1~#3와 PR #3 post-merge Actions가 PASS했다. COLLAB-001은 Task 6 exact runtime evidence와 Task 5/7 teammate rehearsal 때문에 In Progress다.
- 발견한 충돌/부채: remote `feat/web-COLLAB-ONBOARDING-doc-check`가 존재하고 PR은 없다. 감사 중 branch가 `2178405→9df362d→47472d9`로 force-update됐다. 최신 head는 current `main=d54fd6f`에 동기화됐지만 teammate note가 owner local note와 같은 `IMP-20260721-012`를 사용한다.
- Git 상태: local evidence branch는 `origin/main`을 포함하고 이 note 생성 전 clean/ahead 2였다.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 결정/기본값 | 영향 |
|---|---|---|---|---|
| NEXT-001 | Verified risk | teammate branch의 최신 note ID `012`가 owner local note `012`와 충돌 | PR 생성 HOLD; owner `012/013` 통합 뒤 teammate note를 `014`로 변경 | INDEX conflict/duplicate ID 방지 |
| NEXT-002 | Human pending | Cloud exact Node/Python/pnpm/uv runtime output | secret-free read-only Cloud task로 별도 캡처 | COLLAB Task 6 closeout |
| NEXT-003 | Human approval | DATA-SEED-002 execution plan remains Review in `TASKS.md` | 명시적 `계획 승인, 구현 시작` 전 제품 코드 변경 없음 | next backend architecture/data work |
| NEXT-004 | Human pending | `koregy` MFA/recovery readiness | yes/no만 확인하고 코드·복구값은 수집하지 않음 | source-control account risk |

## 5. 설계 결정과 대안

### 선택

1. owner evidence `012/013`을 먼저 integration 대상으로 준비한다.
2. 동시에 사용자는 secret-free Cloud version check를 실행할 수 있다.
3. owner docs가 main에 반영된 뒤 `koregy`가 main을 merge하고 자기 note를 `014`로 바꿔 첫 PR을 연다.
4. 제품 개발은 사용자의 DATA-SEED-002 계획 승인 뒤 별도 branch에서 병렬 재개한다.

### 이유

이 순서는 같은 INDEX 끝행을 여러 branch가 동시에 수정하는 충돌을 직렬화하면서 Cloud/runtime와 backend 작업은 병렬로 유지한다.

### 고려했지만 선택하지 않은 대안

- teammate branch에서 즉시 PR 생성: duplicate `011`과 stale main 때문에 제외.
- teammate branch force-rebase: 이미 remote에 게시돼 normal merge가 더 안전하며 force-push를 기본값으로 삼지 않는다.
- DATA-SEED-002 무승인 구현: active task/plan이 Review 상태라 제외.
- 인간-only 항목이 끝날 때까지 모든 개발 중단: 서로 독립적이므로 제외.

## 6. 구현 상세

| 파일/영역 | 변경 내용 | 이유 |
|---|---|---|
| this note/INDEX | 다음 실행 순서와 remote collision evidence | 요청별 재현 가능한 기록 |
| COLLAB plan/TASKS | teammate branch HOLD와 note `014` reservation | 병렬 작업 충돌 방지 |
| `versions/manifest.json` | docs `2.10.2→2.10.3` | documentation lineage |

### 데이터 흐름/상태 변화

```text
owner docs 012/013 integration
  ├─ Cloud exact runtime read-only evidence
  ├─ DATA-SEED-002 explicit approval → backend implementation
  └─ teammate sync main → rename 011→014 → Draft PR → CI → allowed self-merge
```

### 오류·빈 상태·롤백

- teammate PR에 duplicate ID나 예상 밖 파일이 보이면 merge하지 않는다.
- 이 안내를 되돌릴 때는 note/INDEX/plan/TASKS/manifest 변경만 revert한다.

## 7. 버전 전후

### 생성 시 매니페스트
- product_spec: 2.2.5
- repo_guidance: 1.7.6
- application: 0.3.0-pii-core
- web: 0.2.0-static-chat-shell
- api: 2.0.1-draft
- shared_contracts: 0.2.1
- database_schema: 0.3.0-local
- official_data: 0.0.0-not-populated
- mock_data: 0.0.0-not-populated
- prompt_set: 0.0.2-deepseek-v4-flash-selected
- test_suite: 1.0.0-collaboration
- documentation: 2.10.2

| 축 | Before | After | 변경 이유 |
|---|---|---|---|
| Application | 0.3.0-pii-core | unchanged | 제품 코드 변경 없음 |
| Web | 0.2.0-static-chat-shell | unchanged | frontend 변경 없음 |
| API | 2.0.1-draft | unchanged | 계약/route 변경 없음 |
| DB schema | 0.3.0-local | unchanged | migration 없음 |
| Official data | 0.0.0-not-populated | unchanged | release 없음 |
| Mock data | 0.0.0-not-populated | unchanged | fixture 없음 |
| Prompt set | 0.0.2-deepseek-v4-flash-selected | unchanged | provider 호출 없음 |
| Test suite | 1.0.0-collaboration | unchanged | 테스트 코드 변경 없음 |
| Docs | 2.10.2 | 2.10.3 | next-sequence/collision evidence |

## 8. 명령과 테스트 증거

| 명령/검증 | 결과 | 시간/개수 | 증거 경로 |
|---|---|---|---|
| `git status --short --branch`; `git worktree list --porcelain` | PASS | evidence branch clean/ahead 2; worktree 3 | terminal |
| `rg` over TASKS/COLLAB/DATA-SEED plans | PASS | current blockers/dependencies confirmed | terminal |
| `gh pr list --state all` | PASS | PR #1~#3 merged; teammate PR 0 | GitHub metadata |
| `git ls-remote`; fetch; commit lookup | PASS/dynamic | teammate branch exists and changed `2178405→9df362d→47472d9` during audit; latest parent is `d54fd6f` | remote/GitHub |
| `git diff origin/main...origin/feat/web-COLLAB-ONBOARDING-doc-check` | PASS/finding | latest diff is exactly note+INDEX, but teammate `012` collides with owner local `012` | terminal |

### 미실행 검증과 이유

- 제품/API/DB/browser tests: 제품 변경 없음.
- Cloud exact runtime: 사용자 Cloud UI 실행이 필요한 인간-only Pending.
- teammate CI: PR이 아직 없고 collision correction 전이므로 Pending.
- DATA-SEED-002 tests: 계획 승인 전 구현하지 않음.

## 9. 보안·개인정보·접근성·성능 영향

- Privacy: citizen text/PII/secret 수집·저장 없음.
- Security: MFA는 yes/no만 확인하고 인증·복구값을 받지 않는다. Cloud check는 환경변수 출력과 외부 API 호출을 금지한다.
- Accessibility: UI 변경 없음.
- Performance/cost: Git/GitHub metadata와 문서 검사뿐이며 provider/DB 비용 0.

## 10. 데이터와 출처 영향

- 공식 데이터: unchanged.
- mock/AI 생성: unchanged.
- schema/lineage: unchanged; DATA-SEED-002는 승인 전.
- verified date: 2026-07-21 KST.

## 11. 인간이 반드시 알아야 하거나 승인할 내용

- 지금 `koregy` branch로 PR을 만들면 안 된다. 최신 teammate note `012`와 owner note `012` 충돌을 먼저 고쳐야 한다.
- 다음 제품 구현을 시작하려면 사용자가 `DATA-SEED-002 계획 승인, 구현 시작`을 명시해야 한다.
- Cloud version check와 MFA 확인은 인간 작업이나 backend 개발과 병렬 가능하다.

## 12. AI 내부 구현 세부 — 필요할 때만 보면 되는 내용

- owner note sequence `012/013` 다음 teammate note 번호 `014`를 예약했다.
- teammate branch는 force-rebase 대신 최신 main normal merge 후 충돌 해결을 기본값으로 한다.

## 13. 인수인계·재현·롤백

### 재현

1. `gh pr list --repo tskwak111/Sejong_AI --state all`로 teammate PR 부재를 확인한다.
2. `git fetch origin --prune` 후 `git diff origin/main...origin/feat/web-COLLAB-ONBOARDING-doc-check`를 본다.
3. owner branch의 note `012`와 teammate branch의 web note `012`가 중복임을 확인한다.

### 롤백

이 안내 commit을 revert하고 manifest documentation을 `2.10.2`로 되돌린다. remote teammate branch는 이 작업에서 변경하지 않았다.

### 다음 개발자 시작점

owner docs 012/013 integration을 먼저 완료한다. 이후 teammate에게 main merge + note `014` rename 지침을 전달한다. DATA-SEED-002는 사용자 승인 시 별도 수직 흐름으로 시작한다.

## 14. 남은 위험·미해결 질문·다음 단계

- Human: Cloud exact runtime values, `koregy` MFA/recovery yes/no, DATA-SEED-002 approval.
- Team: stale branch sync, note `014`, first onboarding PR and CI/self-merge rehearsal.
- AI: owner docs integration 준비 후 승인되는 DATA-SEED-002 실행.

## 15. 자체 리뷰

- [x] 요청 충족
- [x] 테스트/검증 — read-only Git/GitHub evidence; final docs/security/diff checks follow
- [x] source-of-truth/계약/버전 동기화 — TASKS/plan/manifest; public contract unchanged
- [x] 개인정보 원문 노출 없음
- [x] 구현 노트 INDEX 갱신
