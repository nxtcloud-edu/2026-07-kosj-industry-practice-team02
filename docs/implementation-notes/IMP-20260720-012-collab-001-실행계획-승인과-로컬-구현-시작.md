# IMP-20260720-012 — COLLAB-001 실행계획 승인과 로컬 구현 시작

- Date/Time (KST): 2026-07-20T22:58:43+09:00
- Task ID: COLLAB-001-EXECUTION
- Type: decision-implementation-platform-security
- Status: In Progress — local Tasks 1~3 approved; external Tasks 4~7 pending human account actions
- Author/Agent: 사용자(실행 승인·외부 계정 확인), Codex(통합·검증), delegated implementer/reviewer agents
- Branch: main
- Base commit: 177dac8
- Related plan/ADR/RFP: COLLAB-001 execution plan, ADR-0019, D-047~D-054, TASKS COLLAB-001

## 1. 사용자 요청과 완료 기준

### 요청

사용자가 `COLLAB-001 계획 승인, 구현 시작`이라고 명시했다. 승인된 private GitHub·Frontend
collaborator·Codex Cloud 협업 전환 계획을 구현하되, 사람이 직접 인증하거나 식별해야 하는 외부
단계는 성공으로 추정하지 않고 로컬에서 안전하게 완료 가능한 검사기·CI/template·문서부터 끝낸다.

### Acceptance Criteria

- D-054와 source-of-truth에 실행 승인을 기록하고 COLLAB-001을 In Progress로 전환한다.
- 전체 Git history 비밀 검사기, 협업 scope/append 정책, 활성 문서/JSON 검사기를 TDD로 구현한다.
- GitHub workflow/template와 Windows/Linux Playwright portability를 secret 0·write token 0 경계로 검증한다.
- 제품 코드·공개 API·DB·공식/mock 데이터·production dependency·public deployment는 바꾸지 않는다.
- 실제 GitHub remote/push/invite/Codex App/Cloud rehearsal은 account 입력·사용자 인증·외부 증거 전까지
  Pending으로 기록한다.
- focused/full feasible gate, diff review, 버전·문서·INDEX 동기화를 마친다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자가 계획/외부 계정을 승인·확인하고 Codex와 위임 에이전트가 로컬 구현·검토 |
| When — 언제 | 2026-07-20 22:58:43 KST 시작; 종료 시각은 최종 검증 뒤 기록 |
| Where — 어디서 | local `main` 기준 격리 worktree, `scripts/`, `.github/`, 협업/운영 문서 |
| What — 무엇을 | private source collaboration의 pre-push·scope·docs·CI 안전장치와 handoff 증거 |
| Why — 왜 | 한 명의 Frontend 팀원과 Cloud Codex가 경계를 넘거나 비밀을 유출하지 않고 병렬 개발하도록 하기 위해 |
| How — 어떻게 | D-054 기록, isolated branches, RED→GREEN TDD, 독립 task/quality review, fail-closed 검사 |
| How much — 어느 정도 | 계획 Task 1~3과 로컬 Task 8; 외부 Task 4~7은 실제 증거 전 Pending, 초기 비용 0원 |

## 3. 시작 전 상태

- 관련 파일: COLLAB-001 spec/plan, ADR-0019, source-of-truth, `.gitignore`, `scripts/`, `.github/`,
  `tools/web-e2e/playwright.config.ts`, 협업 문서와 manifest.
- 기존 동작: local `main` 163 reachable commits, remote 0, collaboration workflow/scope/history/docs
  검사기 0. 기존 current-tree value-redacting PowerShell secret scanner는 존재한다.
- 발견한 충돌/부채: 승인 문서 31개가 아직 미커밋 상태였고 plan/TASKS는 Review였다. 기존
  `check_scope_drift.py`는 approved synthetic fixtures와 ignored `.worktrees` 때문에 현재 환경에서
  알려진 failure가 있어 COLLAB gate 완료 근거로 재사용하지 않는다.
- Git 상태: 시작 branch `main`, base `177dac810468f3cd5aaa4929a971cbde21b4deba`, remote 0,
  staged 0. 기존 dirty 변경은 승인된 collaboration 문서/manifest뿐이며 executable product file은 0.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 결정/기본값 | 영향 |
|---|---|---|---|---|
| COLLAB approval | Human | 8-task 실행계획 착수 | D-054 Approved | scripts/workflows/docs/local commits |
| GitHub identifiers | External input | owner/repo/collaborator login 미제공 | 추정하지 않고 Tasks 4~7 Pending | remote/invite/App/Cloud |
| Product scope | Boundary | 협업 기반 작업이 제품 동작을 바꿀 수 있는가 | 아니오; 새 production dependency/API/DB/data 변경 0 | app/API/DB/data 불변 |

## 5. 설계 결정과 대안

### 선택

승인 문서를 먼저 검증·로컬 commit으로 고정한 뒤 ignored `.worktrees/` 아래 격리 branch에서 세
독립 TDD unit을 병렬 구현한다. 각 unit은 별도 implementer와 reviewer를 거치고 통합 branch에서
workflow/static tests와 전체 feasible gate를 다시 실행한다.

### 이유

현재 dirty 문서와 구현 변경을 섞지 않고 첫 remote commit의 계보를 명확히 하며, history scanner와
untrusted PR scope 처리처럼 보안 민감한 코드를 독립 검토하기 위해서다.

### 고려했지만 선택하지 않은 대안

- dirty `main`에서 병렬 직접 편집: 문서 승인 기준점과 구현 diff가 섞여 리뷰/rollback이 약해진다.
- account identifier 추정 또는 임의 remote 생성: 잘못된 owner/visibility로 외부 상태를 바꾸므로 금지한다.
- GitHub secret에 DeepSeek/DB 값을 넣어 full actual gate 실행: 승인 경계를 위반하므로 제외한다.

## 6. 구현 상세

| 파일/영역 | 변경 내용 | 이유 |
|---|---|---|
| decision/source-of-truth/plan/TASKS | D-054, Approved/In Progress, 외부 pending 경계 | 사용자 승인 계보 |
| implementation note/INDEX/manifest | 실행 기록 시작, repo guidance/docs version 갱신 | 재현·인수인계 |
| `scripts/`, `.github/`, E2E config | 후속 TDD 구현 예정 | 자동화된 협업 경계 |

### 데이터 흐름/상태 변화

제품 데이터 흐름은 불변이다. Git diff path/status만 scope classifier 입력이며 질문/답변/시민
원문과 파일 내용은 출력하지 않는다. 외부 상태는 실제 인증 단계 전까지 변하지 않는다.

### 오류·빈 상태·롤백

invalid SHA/login/path/status, Git 오류와 문서/JSON 오류는 fail closed 한다. Critical history finding은
remote 생성 전 중단한다. 각 구현 unit은 독립 commit이라 cherry-pick revert가 가능하다.

## 7. 버전 전후

### 생성 시 매니페스트
- product_spec: 2.2.5
- repo_guidance: 1.6.1
- application: 0.3.0-pii-core
- web: 0.2.0-static-chat-shell
- api: 2.0.1-draft
- shared_contracts: 0.2.1
- database_schema: 0.3.0-local
- official_data: 0.0.0-not-populated
- mock_data: 0.0.0-not-populated
- prompt_set: 0.0.2-deepseek-v4-flash-selected
- test_suite: 0.9.0-pii-core
- documentation: 2.8.2

| 축 | Before | After | 변경 이유 |
|---|---|---|---|
| Repository guidance | 1.6.1 | 1.6.2 at approval baseline; final value pending | D-054/status sync |
| Application | 0.3.0-pii-core | unchanged | product code 0 |
| Web | 0.2.0-static-chat-shell | unchanged | UI behavior 0 |
| API | 2.0.1-draft | unchanged | contract/runtime 0 |
| DB schema | 0.3.0-local | unchanged | migration 0 |
| Official data | 0.0.0-not-populated | unchanged | data 0 |
| Mock data | 0.0.0-not-populated | unchanged | data 0 |
| Prompt set | 0.0.2-deepseek-v4-flash-selected | unchanged | provider call 0 |
| Test suite | 0.9.0-pii-core | final value pending | collaboration test additions only |
| Docs | 2.8.2 | 2.8.3 at approval baseline; final value pending | D-054/status sync |

## 8. 명령과 테스트 증거

| 명령/검증 | 결과 | 시간/개수 | 증거 경로 |
|---|---|---|---|
| Git branch/HEAD/status/worktree/ignore preflight | PASS | main / `177dac8` / remote 0 / `.worktrees` ignored | terminal |
| plan full read | PASS | 529 lines | terminal |

### 미실행 검증과 이유

- focused/full tests, history scan, workflow parse: 구현 전이며 후속 절에서 실제 결과를 기록한다.
- GitHub remote/Actions/Cloud: account identifier와 사용자 browser authentication가 아직 없다.

## 9. 보안·개인정보·접근성·성능 영향

- Privacy: 기존 author email private-collaborator 공개는 D-053 범위만 유지한다. 비밀값/시민 원문은
  출력·argument·environment·artifact에 전달하지 않는다.
- Security: secret 0, default read-only Actions token, trusted-base policy execution, untrusted-head
  frontend tests 분리를 구현·정적 검증한다.
- Accessibility: 제품 UI 변경 0. 기존 접근성 기준과 E2E를 유지한다.
- Performance/cost: 새 production dependency와 유료 GitHub plan 0. CI는 GitHub Free quota 안에서
  frontend path가 필요할 때만 heavy test를 실행하되 policy summary는 항상 보고한다.

## 10. 데이터와 출처 영향

- 공식 데이터: 불변, 0.0.0-not-populated.
- mock/AI 생성: 불변, 0.0.0-not-populated; 테스트는 synthetic tokens/paths만 사용한다.
- schema/lineage: API/DB/data lineage 불변; source-control decision lineage에 D-054만 추가한다.
- verified date: 2026-07-20 KST; 최종 검증 시 갱신한다.

## 11. 인간이 반드시 알아야 하거나 승인할 내용

- 계획 실행은 승인됐지만 실제 GitHub owner/repo/collaborator login은 AI가 추정할 수 없다.
- browser 인증, private visibility 확인, invite 수락, selected-repository-only Codex App, 첫 Draft PR과
  Frontend self-merge rehearsal은 사용자/팀원이 실제 수행·확인해야 한다.
- 이 작업은 public Web/API/DB 배포, DeepSeek actual 사용 또는 DATA-SEED-002 승인이 아니다.

## 12. AI 내부 구현 세부 — 필요할 때만 보면 되는 내용

- stdlib subprocess argument array, NUL-delimited Git diff parser, JSON-escaped path reporting과
  static YAML assertions의 세부 구현은 승인된 공개 동작을 바꾸지 않는 내부 판단이다.

## 13. 인수인계·재현·롤백

### 재현

승인된 plan Task 1~3의 focused unittest와 최종 verification matrix를 실행하고 Git diff를 검토한다.
외부 단계는 handoff의 정확한 사용자 계정 절차로 별도 재현한다.

### 롤백

로컬 automation은 task별 commit revert로 되돌린다. remote가 생성되기 전에는 외부 rollback이
없다. push 후 remote 제거는 데이터 삭제가 아니므로 collaborator/App revoke와 repo archive/delete는
별도 인간 승인이 필요하다.

### 다음 개발자 시작점

D-054와 plan status를 확인하고, Task 1~3 구현 commit/검증 결과 및 Pending external checklist를
이 노트에서 이어 읽는다.

## 14. 남은 위험·미해결 질문·다음 단계

- GitHub account identifier, authentication, invite/App/Cloud/onboarding rehearsal이 남는다.
- DATA-SEED-002·PII-CONSUMER·deferred `00700`·public deployment는 이 작업 바깥의 기존 상태를 유지한다.

## 15. 자체 리뷰

- [ ] 요청 충족
- [ ] 테스트/검증
- [ ] source-of-truth/계약/버전 동기화
- [ ] 개인정보 원문 노출 없음
- [ ] 구현 노트 INDEX 갱신
