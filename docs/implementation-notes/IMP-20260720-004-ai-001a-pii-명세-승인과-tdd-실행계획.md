# IMP-20260720-004 — AI-001A PII 명세 승인과 TDD 실행계획

- Date/Time (KST): 2026-07-20T10:23:09+09:00
- Task ID: AI-001A-PII-PLAN
- Type: decision-plan
- Status: Decision-only Done / Execution plan Review pending
- Author/Agent: 사용자(명세 승인), Codex(계획·추적·자체 검토)
- Branch: main
- Base commit: `f829343`
- Related plan/ADR/RFP: AI-001A plan, AI-001 spec, ADR-0004, D-041/D-042, P0-06

## 1. 사용자 요청과 완료 기준

### 요청

사용자가 `PII 명세 승인`으로 AI-001 fail-closed PII core written specification을 승인했다.

### Acceptance Criteria

- 명세 승인 시각과 범위를 결정 로그·명세에 기록한다.
- 전체 AI-001 activation과 분리된 `AI-001A` pure core 작업을 TASKS에 만든다.
- 처음 보는 구현자가 TDD로 실행 가능한 exact file/task/command/expected-result 계획을 작성한다.
- fixture를 production code 전 RED commit에서 동결하고 완화 방지 gate를 계획한다.
- A-030/Q-SEED-002, A-021/Q-SEC-003을 해결하거나 D-040을 만들지 않는다.
- 제품 코드·test fixture·dependency·API·DB·data·provider를 아직 변경하지 않는다.
- 계획을 자체/독립 검토하고 사용자 실행 승인 gate에서 멈춘다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자가 정식 명세를 승인했고 Codex가 writing-plans 절차로 AI-001A 계획을 작성했다. |
| When — 언제 | 명세 승인 2026-07-20T10:22:13+09:00; 계획 노트 생성 10:23:09+09:00 |
| Where — 어디서 | spec/plan, DECISION_LOG, TASKS, ambiguity register, versions/changelog, 구현 노트 |
| What — 무엇을 | Q-PII-003 결정 preflight와 순수 PII core 5개 TDD task, RED fixture freeze·검증·rollback을 구체화했다. |
| Why — 왜 | DATA-SEED blocker를 우회하지 않으면서 개인정보 P0의 독립 준비 단위를 안전하게 진행하기 위해서다. |
| How — 어떻게 | exact 파일·내부 타입·정규화·pattern·span·fixture 74 case·명령·예상 결과·commit gate를 명시했다. |
| How much — 어느 정도 | 계획 문서와 추적 문서만 변경했다. 제품 코드·테스트 실행·데이터·network·DB 비용은 0이다. |

## 3. 시작 전 상태

- 관련 파일: approved PII spec, AI-001 discovery, privacy source-of-truth, ADR-0004,
  `apps/api/AGENTS.md`, API pyproject/architecture tests, TASKS, test/DoD/version 규칙
- 기존 동작: runtime privacy package와 PII result fixture가 없고 시민 `/chat` 입력은 비활성이다.
- 발견한 충돌/부채: 전체 AI-001은 DATA-SEED에 Blocked지만 pure core는 I/O 없는 AI-001A로
  분리 가능하다. unresolved PII의 시민-visible response reason은 아직 공개 계약에 없다.
- Git 상태: base `f829343`, 작업 시작 시 clean.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 결정/기본값 | 영향 |
|---|---|---|---|---|
| AI-001A-PLAN | 인간 승인 | 작성한 TDD plan 실행 | 이후 D-043/IMP-005에서 승인됨 | privacy core |
| A-030/Q-SEED-002 | A/Blocker | official seed correction | 미응답; A/B 미구현, D-040 없음 | DATA-SEED/READY/AI activation |
| A-031/Q-PII-002 | B/High deferred | unresolved PII 시민 응답/event reason | A 추천, 답 전 consumer 차단 | 후속 OpenAPI/chat/event |
| A-032/Q-PII-003 | A/Blocker | 공공 대표번호 형태를 보존할지 마스킹할지 | 이후 Q-PII-003=A/D-043으로 해결 | privacy signature/공식 데이터/test |
| A-021/Q-SEC-003 | B/High deferred | public DB function posture | default local-only | public release |

A-032는 독립 계획 검토에서 발견된 새 AI-001A A/Blocker다. A-031은 core에 필요한 enum이 아니라
후속 공개 소비자 계약이므로 이번 plan 실행을 막지 않는다.

## 5. 설계 결정과 대안

### 선택

- AI-001A를 전체 AI-001과 분리한 pure/import-safe vertical preparation slice로 추적
- Task 0 결정 동기화 뒤 5-task TDD: frozen RED fixture → types/Unicode → identifiers → contextual/fail-closed → architecture/full gate
- exact output oracle와 separator/labeled bypass 회귀로 부분·과잉 마스킹 및 알려진 fail-open을 차단
- normalized high-risk span 전체 coverage, Unicode email, explicit context, 독립 3음절 이름의
  보수적 close와 exact 행정 안전어휘를 계획에 고정
- 39 category positive+10 Unicode+5 overlap+20 negative의 exact v1 matrix
- worktree branch `codex/ai-001a-pii-core`, task별 commit, 최종 2단계 독립 review
- 구현 예상 version: application `0.3.0-pii-core`, API package `0.2.0`, tests `0.9.0-pii-core`
- Q-PII-003=A preflight 예상 version: product spec `2.2.3`, docs `2.7.9`; 구현 완료 docs `2.7.10`

### 이유

fixture를 먼저 동결하면 구현에 맞춘 기대값 완화를 막을 수 있다. core에 DB/provider/route를
붙이지 않으면 current blocker와 public contract 결정을 건드리지 않고 PII miss/no-leak를 독립
검증할 수 있다.

### 고려했지만 선택하지 않은 대안

- 전체 AI-001을 즉시 In Progress: DATA-SEED와 consumer reason이 미해결이어서 상태를 과장한다.
- staging validator import: runtime replacement/Unicode/unresolved 계약이 없고 계층을 역전한다.
- fixture를 구현 뒤 작성: false-negative를 구현에 맞춰 숨길 위험이 있다.
- Q-PII-002를 기존 PERSONAL_LOOKUP/503로 임의 매핑: 공개 동작·KPI를 인간 승인 없이 바꾼다.

## 6. 구현 상세

| 파일/영역 | 변경 내용 | 이유 |
|---|---|---|
| PII spec | Review pending→Approved, plan gate 반영 | 사용자 승인 증거 |
| AI-001A plan | decision preflight+5 TDD tasks, exact-output 74-case·10 bypass·Q-PII-003 policy, 명령/commit/rollback | zero-context 실행 가능성 |
| DECISION_LOG | D-042 | 명세 승인과 scope 분리 |
| TASKS | AI-001A Review 추가, parent AI-001 Blocked 보존 | 실제 상태 추적 |
| ambiguity register | A-031/Q-PII-002 B/High deferred | 후속 공개 계약 누락 보존 |
| ambiguity register | A-032/Q-PII-003 A/Blocker | 공공 대표번호와 one-argument core 충돌 보존 |
| versions/changelog | docs plan-stage patch | 계보 |
| IMP-003/004, INDEX | 승인 follow-up와 6W1H | 재현·인수인계 |

### 데이터 흐름/상태 변화

이번 요청은 문서 상태만 `spec Approved / plan Review`로 바꾼다. 계획 실행 후에만
`raw → normalize → detect/select → replace/re-scan → safe|unresolved` core가 생긴다.

### 오류·빈 상태·롤백

계획은 invalid/unsafe/ambiguous/residual을 닫힌 no-text 결과로 나눈다. 문서 단계 롤백은 D-042,
spec status, AI-001A task/plan, A-031/A-032 추적, version/changelog를 같은 commit으로 revert한다.

## 7. 버전 전후

| 축 | Before | After | 변경 이유 |
|---|---|---|---|
| Product spec | 2.2.2 | 동일 | 이미 승인된 설계 의미 불변 |
| Application | 0.2.0 | 동일 | 제품 코드 없음 |
| Web | 0.2.0-static-chat-shell | 동일 | UI 없음 |
| API | 2.0.1-draft | 동일 | wire contract 없음 |
| Shared contracts | 0.2.1 | 동일 | schema/fixture 없음 |
| DB schema | 0.3.0-local | 동일 | DB 없음 |
| Official data | 0.0.0-not-populated | 동일 | data 없음 |
| Mock data | 0.0.0-not-populated | 동일 | data 없음 |
| Prompt set | 0.0.2-deepseek-v4-flash-selected | 동일 | provider/prompt 없음 |
| Test suite | 0.8.2-data-seed-filesystem-gate | 동일 | 계획만 작성 |
| Documentation | 2.7.7 | 2.7.8 | 명세 승인·plan·task/ambiguity 추적 |

## 8. 명령과 테스트 증거

| 명령/검증 | 실제 결과 | 시간/개수 | 증거 |
|---|---|---|---|
| `git status --short`, `git log -3 --oneline` | 시작 clean, base `f829343` | plan 시작 | terminal |
| source/spec/API/test/version read-only 조사 | privacy package 0, 기존 exact tools 확인 | active files | terminal |
| frozen oracle PowerShell 기계 대조 | 74 rows/74 unique, missing 0, extra 0, MASKED 50, SAFE 20, UNRESOLVED 4 | 74 cases | terminal |
| `python -m json.tool versions/manifest.json` | PASS | exit 0 | terminal |
| `python -B scripts/validate_codex_package.py` | PASS, required 12, manifest valid | exit 0 | terminal |
| `scripts/check_secret_patterns.ps1`, `git diff --check` | PASS; secret finding 0, whitespace error 0 | exit 0 | terminal |
| 첫 composite `rg` 진단 | PowerShell quote parse error, read-only·변경 0; single-quote command로 재실행 | exit nonzero→rerun | terminal |
| writing-plans 자체 검토 | placeholder/stale base/lock/version/TDD/oracle/command 보정 | 완료 | plan diff |
| 독립 plan 전체 review | Critical 0 / Important 0, oracle algorithm mismatch 0 | final plan | independent reviewer |
| 독립 Task 1~4 regex/TDD review | Critical 0 / Important 0, 74 oracle·10 bypass·Q policy 논리 GREEN 가능 | final plan | regex reviewer |
| final fresh documentation gate | package 12 PASS, secret/diff PASS, oracle 74/74·50/20/4, protected path 0, D-040 0/D-042 1, docs 2.7.8·비문서 version 불변 | exit 0 | terminal |

### 미실행 검증과 이유

API pytest/Ruff/mypy/root build는 제품 코드·fixture·dependency를 변경하지 않은 plan 단계라
실행하지 않는다. exact commands와 기대 결과를 plan Task 0~5에 기록했으며 Q-PII-003 결정과
실행 승인 뒤 수행한다.

## 9. 보안·개인정보·접근성·성능 영향

- Privacy: actual PII 0, synthetic matrix만 계획. exact-output oracle, pre-mask full-span coverage,
  partial leak·self-token·Unicode/explicit context 회귀를 계획했고 원문 result/log/provider/DB 금지 유지.
- Security: worktree, stdlib-only AST gate, no I/O/import, secret/protected scope를 계획했다.
- Accessibility: UI 변경 없음.
- Performance/cost: bounded 1000자×100회/2초 gate. 새 dependency·외부 호출·예산 0원.

## 10. 데이터와 출처 영향

- 공식 데이터: 변경/조회/승격 0; `official_data=0.0.0-not-populated`
- mock/AI 생성: 구현 때 만들 synthetic test fixture만 계획; 이번에는 생성 0
- schema/lineage: API/DB/official release lineage 변경 0
- verified date: 2026-07-20

## 11. 인간이 반드시 알아야 하거나 승인할 내용

- 정식 명세는 승인됐지만 **실행계획은 아직 Review**다. `계획 승인, 구현 시작` 전 code 0.
- Q-PII-003은 이번 core 계획 blocker다. 추천 A는 시민 입력의 “공식” label을 신뢰하지 않고
  모든 phone-shaped value를 마스킹하는 보수적 선택이다.
- Q-PII-002는 후속 시민 응답 reason 결정이며 core 구현 blocker는 아니다. 추천은 A다.
- Q-SEED-002와 Q-SEC-003은 별도 blocker로 그대로 남는다.
- production dependency/API/DB/data/provider/public deployment는 이 계획에 포함되지 않는다.

## 12. AI 내부 구현 세부 — 필요할 때만 보면 되는 내용

- exact regex helper, candidate tuple, span ordering, pytest parameterization
- 3음절 standalone name safe lexicon과 읍·면·동 suffix 제외 helper
- Ruff formatting과 private helper 분리
- 계약을 약화하지 않는 synthetic negative case 추가

## 13. 인수인계·재현·롤백

### 재현

1. base `f829343`에서 이번 문서 commit diff를 확인한다.
2. approved spec 각 section을 plan Task 0~5와 대조한다.
3. D-040 row 0, D-042 row 1, A-031 deferred, A-032 plan blocker, AI-001A Review/AI-001 Blocked를 확인한다.
4. manifest `documentation=2.7.8`, 다른 축 불변을 확인한다.

### 롤백

제품/DB/data가 없으므로 이번 documentation commit 하나를 revert한다. plan만 지우고 D-042/task/
spec status를 남기는 부분 rollback은 하지 않는다.

### 다음 개발자 시작점

사용자가 Q-PII-003=A와 계획 승인을 함께 명시하면 먼저 plan Task 0에서 D-043·spec
`2.2.3`·A-032 Resolved·IMP-005를 문서 전용 commit으로 고정한다. 그 clean SHA에서
`superpowers:using-git-worktrees`, `superpowers:subagent-driven-development`,
`superpowers:test-driven-development`를 읽고 Task 1 RED fixture를 실행한다. 승인 전 `apps/api`를
수정하지 않는다.

## 14. 남은 위험·미해결 질문·다음 단계

- plan pattern과 synthetic case가 실제 RED/GREEN에서 충돌할 수 있다. fixture 기대 완화 없이
  구현을 고치며 privacy 의미 변경이면 중단한다.
- Q-PII-003 공공 대표번호 처리는 인간 결정과 계획 승인을 함께 받아야 한다.
- Q-PII-002 response/event reason은 consumer slice 전 인간 결정이 필요하다.
- A-030/Q-SEED-002 때문에 official ACTIVE/READY와 전체 AI-001은 Blocked다.
- 독립 plan review는 Critical 0 / Important 0으로 완료됐다.
- 후속 상태: Q-PII-003=A와 plan 승인은 D-043/IMP-005에서 기록됐다. 다음은 Task 0 권위
  동기화 commit 뒤 isolated TDD implementation이다.

## 15. 자체 리뷰

- [x] 요청/승인 기록 충족
- [x] exact plan 작성; 제품 테스트 미실행 사유 기록
- [x] source-of-truth/결정/task/ambiguity/버전 동기화
- [x] 개인정보 원문·실제 데이터·provider/DB 접근 없음
- [x] 구현 노트 INDEX 갱신
